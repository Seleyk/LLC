import { Box } from "@mui/material";
import { styled } from "@mui/material";

const NavContainer = styled(Box)({
    backgroundColor: "#ffffff",
    boxShadow: "0 1px 10px 0 rgba(0,0,0,.14902)",
});

export default NavContainer
